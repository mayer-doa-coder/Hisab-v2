{
  "name": "@hisab/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Pure TypeScript domain layer. Zero I/O. See AGENTS.md §3.1.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo 'packages/domain: no tests yet (Step 2 has not started)' && exit 0"
  }
}
